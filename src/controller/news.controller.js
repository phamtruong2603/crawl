import { News } from "../entities/news";

const newsController = {
    createNews: async(req, res) => {
        try {
            
        } catch (error) {
            console.error(error)
        }
    }
}